import { Stack } from "react-bootstrap";
import { Blog } from "../types/blog";

export default function CommunityAction({blog}:{blog:Blog}) {
    return (
        <Stack direction="horizontal" >
            <div></div>
        </Stack>
    );
}